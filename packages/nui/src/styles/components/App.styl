.n-app-bar {
  min-height: 56px;
  margin: 0;
  position: fixed;
  width: 100vw;
  z-index: 1000;
  display: flex;
  align-items: center;
}

.n-app-bar+.n-app-main {
  position: relative;
  top: 56px;
}

.n-app-main {
}